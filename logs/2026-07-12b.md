# 2026-07-12 (devam)

- **`parseSeason` sessiz yok sayma sorunu çözüldü:** `app/drivers/[driverId]/page.tsx` ve `app/teams/[constructorId]/page.tsx` — fonksiyon artık `{ season, requestedUnsupported }` döndürüyor. Kullanıcı `?season=2024` gibi desteklenmeyen bir değer denerse sayfada görünür bir uyarı banner'ı çıkıyor ("Only the {season} season is available right now — showing current data instead"), önceden tamamen sessizce yok sayılıyordu.
- **Kalan borç kalemleri gözden geçirildi:** `frame-ancestors` (`*.vercel.app`) ve cron `maxDuration` tekrarı bilinçli/düşük riskli kabul edildi, dokunulmadı — ikisi de mevcut yorumlarla zaten gerekçelendirilmiş. Renk-kodlu SVG `aria-label` maddesi için kod tabanında ilgili bileşen bulunamadı (muhtemelen önceki bir temizlikte kaldırılmış) — madde kapatıldı.
- **Rapor güncellendi:** `docs/reference/web-iyilestirme-onerileri-2026-07-05.md` — tüm 🔴/🟠 ve `parseSeason` maddesi kapalı. Geriye yalnızca RTL component testleri, `rateLimit` Upstash yolu testi, ve `profileSeasons()` N+1 riski (tarihsel sezon desteği gelince) kaldı.
- **Doğrulama:** `tsc --noEmit` temiz, `npm test` 117/117 geçti, `npm run lint` yalnızca `mobile/` ve `old-versions-valuable-files/`'daki (bu değişiklikle ilgisiz, önceden var olan) hatalar/uyarılar gösterdi.
