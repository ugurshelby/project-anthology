# AGENT LOG — 5 Fix (Bölüm 1)

Tarih: 2026-06-08
Plan: `docs/plans/PLAN_5FIX_2026-06-08.md` (Bölüm 1)

## Ne yapıldı

### Sorun 1 — Season standings freshness
- `app/season/page.tsx`: `revalidate = 900` → `revalidate = 0` + `export const dynamic = 'force-dynamic'`.
- Sonuç: `/season` artık home ile aynı per-request staleness→canlı Jolpica yolunu kullanıyor; build çıktısında `ƒ (Dynamic)`.

### Sorun 2 — Past Winners (F1DB schema fix) — EN BÜYÜK
- Kök neden doğrulandı (canlı F1DB JSON probe): result dizisi `raceResults` (kodda `driverResults`), sprint `sprintRaceResults`, item'lar düz ID-referanslı, isimler top-level `drivers[]`/`constructors[]` lookup'larında. Standings + qualifying mapper'ları da aynı kırık nested alanları okuyordu.
- `lib/f1/sources/f1db.ts`:
  - Interface'ler gerçek F1DB şemasına göre yeniden yazıldı (`F1DbRace`, `F1DbDriverResult`, `F1DbQualResult`, `F1DbStanding`, yeni `F1DbDriver`/`F1DbConstructor`).
  - `F1DbData`'ya `drivers`/`constructors` eklendi; `getF1DbLookups(db)` (id→record Map, memoized) + `clearF1DbCache` lookup'ı da temizliyor.
  - Yeni resolver'lar: `ergastDriver`/`ergastConstructor`/`ergastPosition`. `driverResultToErgast`/`qualResultToErgast` lookup parametresi alıyor; isimleri id'den çözüyor. `position` string ("1") üretiyor.
  - `toMRDataResults`/`toMRDataQualifying`/`toMRDataSprint`/`toMRDataDriverStandings`/`toMRDataConstructorStandings` lookup parametresi alacak şekilde güncellendi; `raceResults`/`sprintRaceResults` okuyor.
- `scripts/seed-f1-history.ts`: `getF1DbLookups` import + `lk` tüm mapper call site'larına geçiriliyor.
- Doğrulama: dry-run `--from 2023 --to 2023` → 22 round'un hepsinde `results` satırı (önce sıfırdı), 53 upsert / 0 hata. Mapper probe: 2023 R1 = Max Verstappen/Red Bull, 2018 R1 = Sebastian Vettel/Ferrari (dolu isimler).

### Sorun 3 — Race Calendar UX
- Yeni `components/season/CalendarScroller.tsx` (`'use client'`): mount'ta `data-nearest="true"` kartı ortalar (scrollLeft).
- `app/globals.css`: `.race-calendar-scroll` (scrollbar-width:thin, scrollbar-color #ff1801/#131313 + webkit height/thumb/track).
- `app/season/page.tsx`: en yakın yarış round'u hesaplanıyor (`argmin |date - now|`, geçersiz tarih atlanır), scroll div `CalendarScroller` ile sarıldı, nearest karta `data-nearest` eklendi.

### Sorun 4 — GitHub workflow (devre dışı)
- `.github/workflows/sync-f1-race-aware.yml`: `schedule` trigger'ı kaldırıldı, sadece `workflow_dispatch` kaldı. Açıklama yorumu: Vercel cron baseline'ı çalıştırıyor; yeniden açmak için `CRON_SECRET_KEY` secret + `SITE_URL` var gerekli.

### Sorun 5 — News live RSS + 15 dk cache
- `lib/data/news.ts`: `hasRealImage` export edildi (param `{ image: string }`).
- `lib/news/aggregate.ts`: gövde `aggregateUncached()`'e taşındı (tam set, slice yok); `aggregate()` 15 dk module-scope cache + in-flight stampede guard + serve-stale-on-error ile sarıldı, sonucu `maxItems`'a slice ediyor.
- `app/news/page.tsx`: `getLatestNews`/`getFeaturedNews` yerine `aggregate({maxItems:100})`; featured = ilk gerçek görselli item. `revalidate=0` korundu.

## Değişen dosyalar
- `app/season/page.tsx`
- `lib/f1/sources/f1db.ts`
- `scripts/seed-f1-history.ts`
- `components/season/CalendarScroller.tsx` (YENİ)
- `app/globals.css`
- `.github/workflows/sync-f1-race-aware.yml`
- `app/news/page.tsx`
- `lib/news/aggregate.ts`
- `lib/data/news.ts`

## Çalıştırılan komutlar
- `npm run build` — 2 kez, sıfır hata (✓ Compiled successfully, exit 0). `/season` ve `/news` = Dynamic.
- `npx tsx scripts/seed-f1-history.ts --dry-run --from 2023 --to 2023` — 53 upsert / 0 hata, results satırları görünür.
- F1DB şema + mapper probe'ları (tmp tsx, commit edilmedi).

## Karşılaşılan hatalar ve çözümleri
- F1DB şeması beklenenden derin uyumsuzdu (sadece dizi adı değil — tüm result/standing/qual item'ları ID-referanslı). Çözüm: lookup map mimarisi + tüm mapper'ların yeniden yazımı.
- tmp probe scriptleri `@/` alias için proje kökünden çalıştırıldı (relative path /tmp'den çözülmüyordu).

## Sonraki adım
⚠️ MANUEL AKSİYON: Tarihsel results'ı DB'ye yazmak için bir kez çalıştır:
`npx tsx scripts/seed-f1-history.ts`
(.env.local'da NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY gerekli). Bu çalışana kadar Past Winners tarihsel sezonlarda boş kalır. Upsert idempotent; qualifying/standings'i de doğru isimlerle yeniden yazar.

Ardından Bölüm 2 (4 özellik) uygulanabilir.
