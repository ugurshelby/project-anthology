# 2026-07-11

- **Railway cron kurulumu tamamlandı:** `railway/apex-sync-f1-cron/` (ana repo içinde alt dizin, ayrı reponun yerine) — Railway servisinin Root Directory'si buraya işaret ediyor. `SITE_URL` ve `CRON_SECRET` env var'ları set edildi, `*/15 * * * 5,6,0` cron schedule (Cuma/Cumartesi/Pazar, 15 dakikada bir) yapılandırıldı. İlk denemelerde `DEPLOYMENT_NOT_FOUND` hatası alındı — sebep, Efendim'in Vercel'de projeyi yeniden oluşturmasıyla prod domaininin `project-anthology-five.vercel.app` → `project-anthology-seven.vercel.app` olarak değişmesiydi. `lib/data/siteUrl.ts`'deki hardcoded fallback + Railway'deki `SITE_URL` güncellendi, "Run now" ile doğrulandı: `[sync-f1 live] 200 OK`.
- **Pilot detay hero güçlendirildi:** `ProfileHero.tsx` — portrenin arkasında (masaüstü, `lg:flex`) daha büyük ve team rengiyle stroke'lu bir numara katmanı, mobilde portrenin üstünde daha küçük ayrı bir rozet (`lg:hidden`). Team hero'daki flank-numbers formatına dokunulmadı.
- **Tech glossary mobilde accordion:** Yeni `components/glossary/GlossaryAccordion.tsx` (client component) — mobilde her terim artık tıklanabilir açılır/kapanır kart, masaüstünde mevcut `dl` definition-list layout korunuyor. `tsc`/`eslint`/54 test + dev server HTML çıktısı ile doğrulandı (Chrome DevTools MCP bu ortamda yok, görsel doğrulama yapılamadı — Efendim deploy sonrası tarayıcıda kontrol edecek).
- **`docs/reference/web-iyilestirme-onerileri-2026-07-05.md` güncellendi:** `sync-f1` scope-forcing maddesi ✅ çözüldü olarak işaretlendi (Railway cron), tasarım dili bölünmesi maddesine pilot hero/glossary notu eklendi. Kalan tek açık orta öncelik: `loading.tsx`/`error.tsx`/`not-found.tsx` eksikliği.
