# 2026-07-15 — Mobil bug fix + push bildirimleri turu (plan uygulaması)

Onaylanan plan `docs/superpowers/plans/2026-07-13-mobile-bugfix-and-features.md`
sırayla uygulandı. Part A (13 bug), Part B (Grid/Season ayrımı) ve Part C
(pist haritaları) gün içinde tamamlanmıştı; bu oturumda **Part D (push
bildirimleri gönderme tarafı)** bitirildi.

## Part A — 13 bug (tamamlandı)

Web (root repo): A2 tekrarlayan anthology hero görseli (`085d045`, 14 hikaye),
A3 kart altyazısı kelime sınırında kesme (`a75a350`), A4 kart gradient
güçlendirme (`8f7a992`). imola-1994 için görsel istisnası belgelendi
(`91165fb` — yeni fotoğraf gerekiyor). Mobil (bağımsız repo): A1 Lindblad
permanentNumber (`68cbc28`), A3/A4 mobil kopyaları, A5 detay başlık kontrastı,
A6 takım logo kontrastı (açık chip zemin), A7 DriverCard puan rengi (P4+ artık
nötr), A8 tek-tip geri sayım formatı, A9 Anthology/News tab ikon ayrımı,
A10 siyah news kartı fallback + 5 kopya geri butonu → tek `BackButton`.

## Part B — Grid/Season ayrımı (tamamlandı)

Mobil: Season sekmesi kompakt liste görünümüne geçti (`9e7a86d`) — Grid'in
kart/fotoğraf görünümünden ayrıştı.

## Part C — Pist haritaları (tamamlandı)

Mobil: `expo-blur` eklendi (`3e73e5f`), circuit detay pist haritası kartı
glassmorphic — gerçek pist fotoğrafı zemininde blur cam efekti (`49765ec`),
web ile parite.

## Part D — Push bildirimleri gönderme tarafı (bu oturum)

Keşif: client + backend registration tarafı önceki turdan zaten hazırmış
(`lib/notifications.ts`, `app/api/push/register/route.ts`, `push_subscriptions`
tablosu, `expo-server-sdk`). Sadece **gönderme** tarafı eksikti.

- **D1** — `notified_sessions` dedupe tablosu migration'ı yazıldı ve
  commit'lendi. Round-NULL değil, session bazlı `(season, round, session_type)`
  unique. Migration `20260606000001_partial_unique_index.sql`'in round-NULL
  garantisi doğrulandı; `ingestSeasonSnapshot`'ın update-in-place davranışı
  teyit edildi.
- **D2** — `app/api/cron/notify-sessions/route.ts` + `lib/push/sendExpoPush.ts`.
  Seans başlangıcından ~30dk önce, opt-in aboneye push. Planın inline session
  hesabı yerine mevcut `sessionStartMs`/`raceStartMs` helper'ları kullanıldı
  (12:00:00Z fallback tek kaynakta). Yalnız qualifying/sprint/race — FP1-3
  verisi ingest pipeline'ında yok, mobil toggle'ları görünür ama inert.
- **D3** — `sync-f1` artık şampiyonluk lideri (pilot + takım) değişince tek
  bildirim gönderiyor. Kritik: eski lider, snapshot üzerine yazılmadan ÖNCE
  okunuyor (history tablosu yok). Mobil bildirim ekranına "Championship lead
  changes" (`standings`) toggle'ı eklendi; `NotificationPrefs` tipi tanımlandı.

Doğrulama: web `tsc --noEmit` + `npm run build` temiz (`/api/cron/notify-sessions`
route derlendi), mobil `tsc --noEmit` temiz.

## İki onay kapısı (Efendim onayladı, tamamlandı)

1. **Migration canlı Supabase'e uygulandı.** `supabase db push`. Not: uzak
   migration geçmişinde `push_subscriptions` (önceki tur elle oluşturulmuş)
   kayıtlı değildi → `migration repair --status applied 20260625000001` ile
   işaretlendi, sonra `notified_sessions` temiz uygulandı. Artık yerel = uzak.
2. **D4 — Railway cron kalıbı repo'ya eklendi** (`railway/apex-notify-sessions-cron/`,
   sync-f1'in birebir kardeşi). Servisin/schedule'ın Railway panelinden
   kurulumu Efendim'de. `vercel.json`'a satır eklenmedi.

## Kalan tek manuel iş (Efendim'de)

Railway panelinden `apex-notify-sessions-cron` servisini oluşturup 5-10dk'lık
cron schedule + `SITE_URL`/`CRON_SECRET` env değişkenlerini eklemek. Kod ve
altyapı kalıbı hazır.

Web repo'da bu turun commit'leri + önceki 7 commit henüz origin'e push
edilmedi (Efendim istediğinde).
