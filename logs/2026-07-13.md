# 2026-07-13 — Mobil + web: 7 kreatif özellik turu

Efendim'in önceki gece istediği 26 maddelik redesign turundan sonra ("yarın test
edeceğim, sen şimdi bana ... kreatif fikirler ... gel") sunduğum 12 fikirden
Efendim'in seçtiği 7'si + bildirilen bir bug, tam otonom onayla ("kaldığın
yerden devam et... hepsine birer commit et, hepsi bitince push et") kolaydan
zora sırayla uygulandı.

## Önce: bug fix

- **News kartı tıklama bug'ı** (mobile `4f7db97`): genişleyen haber kartında
  `height: undefined` olan container içindeki `Image`'ın `height: '100%'`'ü
  referans bulamayıp anlamsız biçimde büyümesi — image artık sabit yükseklikte
  kalıyor, sadece özet/buton bölümü genişliyor.

## 7 özellik (kolaydan zora)

1. **Anthology gerçek içerik** (mobile `e657053`): web'in `stories` tablosu
   hiç seed edilmemiş — 17 tam yazılmış hikaye + 120 görsel dosyası zaten
   `data/stories/content.ts` ve `public/stories/`'de hazırdı, sadece DB'ye
   işlenmemişti. `scripts/seed-stories.ts` çalıştırıldı (idempotent upsert).
   Mobile'ın `fetchStories`/`fetchStory`'si yanlış kolon şemasını (kicker/
   cover_image_url/excerpt) varsayıyordu — gerçek jsonb şemasına göre
   yeniden yazıldı, hikaye detay ekranı web'in editöryel blok tiplerini
   (heading/quote/image/paragraph + drop-cap) mobilde parallax hero ile
   render ediyor.

2. **Haptic geri bildirim** (mobile `ed9c136`): `expo-haptics` + merkezi
   `lib/haptics.ts` — `PressableCard` (tüm kartların ortak temeli), tab bar
   geçişleri, haber kartı genişletme. Ses geri bildirimi bilinçli olarak
   şampiyonluk ekranına ertelendi (her tıklamada ses rahatsız edici olurdu).

3. **Takım DNA'sı** (mobile `2f70631`, web `08f81cc`): 11 takım için
   karakteristik tileable mikro-doku (Ferrari diagonal, Mercedes grid,
   Red Bull chevron, vb.) — mobilde `react-native-svg` Pattern, webde CSS
   `repeating-linear-gradient` karşılığı (`lib/assets/team-pattern.ts`).
   Standings satırları, Grid kartları, takım detay hero'sunda düşük
   opaklıkla renk gradyanının üstünde.

4. **Sezon sonu şampiyonluk ekranı** (mobile `4f90dfa`): tüm yarışlar bitip
   lider net farkla 1. olduğunda tam ekran confetti + takım renginde
   kutlama overlay'i — `AsyncStorage` ile yıl bazlı "görüldü" kaydı, her
   açılışta tekrar çıkmıyor.

5. **Design System birleştirme — web mobile'a paritye** (web `08f81cc`):
   gerçek bir monorepo paket birleştirmesi yerine (Next.js/Metro build
   farkı nedeniyle riskli olurdu), web'in standings kartlarına mobile'da
   zaten var olan takım logosu + Takım DNA dokusu eklendi — iki platform
   artık aynı görsel dili paylaşıyor.

6. **Race Day Mode** (web `9dd38be`, mobile `fc23d71`): yeni
   `/api/season/[year]/last-result` endpoint'i (isRaceWeekend tespiti +
   en son biten yarışın podyum/fastest-lap özeti, `lib/f1Calendar.ts`'deki
   mevcut `isRaceWeekend`/`getLastFinishedRace` yardımcılarını kullanıyor).
   Mobile ana sayfasında pulsing "RACE WEEKEND" rozeti + podyum kartı.

7. **Android widget — ERTELENDİ**: Expo'da resmi desteklenmeyen native bir
   özellik (Kotlin `AppWidgetProvider` + ayrı Gradle modülü + config plugin,
   `react-native-android-widget` gibi bir topluluk paketi + `expo prebuild`
   gerektiriyor). Efendim'e durumu bildirdim, "şimdilik atla, ayrı oturuma
   bırak" onayı aldım — bu turun dışında tutuldu, mevcut 7 özellik güvenle
   teslim edildi.

## Doğrulama

- Her adımda mobile: `tsc --noEmit` + `expo export --platform android` temiz.
- Web: `tsc --noEmit` + `npm run build` temiz (build log'unda `stories`
  sorgusunun artık 17 satır döndüğü görüldü).
- Web push edildi (`9dd38be`), Vercel deploy doğrulandı (`/api/season/2026/
  last-result` prod'da 200 dönüyor).
- Mobile repo (bağımsız, remote yok) 7 ayrı commit ile güncel.

## EAS Build

- Build ID: `32e1361a-0c5c-4e8b-affb-9012dd23dfab` — status FINISHED
- Commit: `fc23d71` (Race Day Mode, son commit) — build'in `gitCommitHash`'i ile doğrulandı
- APK: https://expo.dev/artifacts/eas/uMmpvJ9TpyUaW5FZYft2-q_i-kZxUF06Nf3Eo2x_QKg.apk

## Sıradaki adım

Efendim APK'yı test edecek. Android widget ayrı bir oturumda ele alınacak —
`react-native-android-widget` paketiyle denenip denenmeyeceği veya sahte/
mockup bir "widget önizleme" ekranı mı yapılacağı henüz karara bağlanmadı.

---

# Aynı gün devam — Web Redesign Round 2 (8 maddelik spec)

Efendim canlı web sitesini ekran görüntüleriyle inceleyip sayfa sayfa detaylı
geri bildirim verdi (INP performans sorunu, haber 404 bug'ı, homepage, season,
drivers/teams+detay sayfaları, circuits+detay, anthology detay, navbar).
Her mesajda "sıraya ekle, aksiyon alma" talimatıyla topladım, sonunda
"tüm listeyi bir spec yap" dedi. Spec yazıldı: `docs/superpowers/specs/
2026-07-13-web-redesign-round2-design.md` (commit `a25731b`). Ardından
"onaylıyorum uygulamaya başla tam yetkin var" onayı geldi — tam otonom,
her maddeye ayrı commit, sırayla uygulanıyor. **Bu turda push/deploy/EAS
build istenmedi** (mobil turdan farklı) — sadece local commit'ler.

## Tamamlanan maddeler (4/8)

1. **INP performans sorunu** (`219843e`): standings hover'daki
   `background-color`+`translate-y` kombinasyonu layout/paint tetikliyordu
   (DevTools: 256ms render/261ms INP) — sadece `transform`+`opacity`'e
   taşındı (compositor-only).

2. **Haber detay 404 bug'ı** (`9a148c9`): kök neden bulundu — `/news` listesi
   canlı `aggregate()`'in ürettiği `stableId(canonicalUrl)` id'sini
   kullanıyordu, `/news/[id]` detay sayfası ise `news_cache` DB satırının
   kendi otonom id'sini (`Supabase`'in primary key'i, `stableId`'den tamamen
   bağımsız) döndürüyordu — iki id hiç eşleşmiyordu. `stableId`/`canonicalize`
   export edildi, `newsFromCache()` artık aynı hesaplamayı yapıyor.

3. **Homepage redesign** (`3260b00`): `SplitHomeLayout`/`HomeDataColumn`
   silindi (ölü kod), yerine asimetrik `BentoGrid`: Next Race hero (7/12,
   `PosterHero`'ya yeni `contained` prop) + yeni `LatestRaceCard` (5/12,
   podyum+fastest lap) üst sırada; Driver Standings 5 pilot büyütülmüş
   avatarlarla (7/12, `FlatStandingsList`'e yeni `avatarSize` prop) + News
   (5/12) alt sırada. Lider portresi opaklığı 40%→90%.

4. **Season redesign** (`44310cd`): Calendar tam genişlik tek başına üstte.
   Pist haritaları büyütüldü (w-12→w-20, opacity 70→95). Yeni
   `RoundsProgress` (dolgu bar) düz "9/22" yerine. `StandingsCard`
   Drivers/Teams toggle'ından ayrı iki bento karta bölündü (yeni
   `TeamsStandingsCard`, `StandingsToggle` silindi). Yeni `MostWinsCard`
   (takım logosu+renk glow). Last Race bloğu homepage'in `LatestRaceCard`'ını
   reüze ediyor — `PodiumViz` artık kullanılmadığı için silindi.

## Doğrulama (her madde)

`tsc --noEmit` + `npm run build` temiz (Google Fonts fetch ara sıra geçici
hata veriyor, tekrar denemek yeterli). Her madde kendi commit'i ile `main`'e
işlendi, push edilmedi.

## Tamamlanan maddeler (5-8) — turun tamamı bitti

5. **Grid sayfası + detay sayfaları** (`3886d3b`): `/drivers` + `/teams` hub
   sayfaları tek `/grid`'e birleşti (her takım grubu: solda 2 pilot kartı,
   sağda takım kartı, simetrik/ortalanmış) — eski route'lar redirect ediyor.
   `DriverCard` tam-boy portre + baskın renkle yeniden tasarlandı. Driver
   hero'su ortalandı (eski asimetrik `grid-cols-[1.4fr_1fr]` "sola yatık"
   şikayetinin kök nedeniydi). Yeni `LoreSection` — hem driver hem team
   detay sayfaları `DriverLore`/`TeamLore` verisini zaten çekiyordu ama
   sadece `.number`'ı kullanıyordu, bio/milestones/lore metni hiç
   render edilmiyordu.

6. **Circuits sayfası + detay** (`02cd51e`): `CIRCUIT_GRID_SPANS`/
   `circuitGridSpan` (zaten yazılmış ama hiç çağrılmıyormuş!) artık gerçekten
   asimetrik kart boyutlandırma sağlıyor. Kartlarda gerçek pist fotoğrafı +
   ülke bayrağı (yeni `lib/data/countryFlags.ts`). Sayfa açılışında next-race
   kartına otomatik scroll. Detay sayfada "Driver Standings" (circuit ile
   alakasız, /season'ı tekrarlıyordu) → "Results" (sezonun tüm tamamlanan
   yarışları, scroll edilebilir podyum kartları, yeni
   `getCurrentSeasonResults`). Recent Winners son 5 yıl (8'den düştü).
   Pist haritası kartı glassmorphism (backdrop-blur 20px + rim highlight).
   Yeni `CircuitCharacter` — mobile'daki circuit facts içeriği.

7. **Anthology hikaye detay redesign** (`a03cc2d`): `AnthologyHero` artık
   `min-h-[70vh]` full-bleed değil — üstte metin, altta 16:9 çerçeveli kart.
   `StoryBody`'nin image bloğu artık `100vw` değil, okuma sütunu genişliğinde
   rounded/bordered kart; portrait görseller 3:4 oranında.
   **Görsel spesifikasyonu Efendim'e teslim edildi:**
   - Hub kart kapağı: 16:9, min 1200×675px
   - Hikaye detay hero: 16:9, min 1600×900px
   - İçerik görselleri (full/landscape): 16:9, min 1600×900px
   - İçerik görselleri (portrait): 3:4, min 1200×1600px

8. **Navbar redesign** (`26c0f12`): masaüstünde (lg+) APEX logosu ortada,
   3 öğe solda (Season·Grid·Circuits), 3 öğe sağda (News·Anthology·Glossary).
   Glossary artık header'da (önceden sadece footer'da). Mobile tab-bar
   Drivers→Grid güncellendi, "more" menüsünden Teams kaldırıldı.

## Sonuç

8/8 madde tamamlandı, her biri ayrı commit ile `main`'e işlendi. **Push
edilmedi** — bu turda kullanıcı push/deploy istemedi (mobil turdan farklı).
Her adımda `tsc --noEmit` + `npm run build` temiz.

Detaylı devam notu: hafıza `web-redesign-round2-progress.md`.

---

# Aynı gün devam — Web navbar regresyon fix'i

Push sonrası (`07f0675`) Efendim mobilde masaüstü navbar'ının da göründüğünü
bildirdi. Kök neden: navbar redesign'da (`26c0f12`) md-only fallback
`HeaderNav` çağrısı `className="flex lg:hidden"` yazılmıştı — `flex`
breakpoint'siz oldu için taban stildi, mobilde de görünüyordu. Ayrıca
`SiteHeader` sadece ana sayfada mobilde gizleniyordu, diğer sayfalarda boş
logo şeridi kalıyordu. Fix: `hidden md:flex lg:hidden` + header artık her
sayfada koşulsuz `hidden md:block`. Commit `694f11f`, push edildi, Vercel
deploy doğrulandı (`/season` 200 dönüyor).

---

# Aynı gün devam — Mobil bug fix + yeni özellikler turu (SPEC ONAYLANDI, uygulama akşama ertelendi)

Efendim APK'yı test edip ekran görüntüleriyle 13 maddelik bug listesi +
yeni özellik istekleri verdi. Sezon arşivi bilinçli olarak dışarıda
bırakıldı (büyük DB işi, ayrı oturum). Brainstorming skill'i ile
netleştirme yapıldı, spec yazılıp commit edildi:
`docs/superpowers/specs/2026-07-13-mobile-bugfix-and-features-design.md`
(commit `1c1dd43`).

**Kapsam netleşti:**
- 13 bug doğrudan uygulanacak (onay gerektirmiyor) — logo kontrastı,
  Lindblad numarası, anthology tekrarlayan hero görsel, kart truncate,
  siyah news kartı, kaynak tekrarı, grid tutarsızlığı, puan renk kodlaması
  (P2 kırmızı → nötr renk), geri sayım format, navigasyon geri ok
  tutarlılığı, news/anthology ikon benzerliği.
- Grid vs Season ayrımı: Grid = kart/profil görünümü, Season = kompakt
  liste — web'deki mevcut ayrımın mobile'a parite'lenmesi.
- **Push bildirimleri**: keşif sonucu client+backend registration tarafı
  zaten önceki turda tam bitmiş (`lib/notifications.ts`,
  `app/api/push/register/route.ts`, `push_subscriptions` tablosu,
  `expo-server-sdk` kurulu) — sadece **gönderme** tarafı eksik. Yeni
  `/api/cron/notify-sessions` (session başlangıcı, ~30dk önce) +
  mevcut `sync-f1` cron'una şampiyonluk lideri değişince tek bildirim
  eklenecek.
- **Pist haritaları**: mobile circuit detay sayfası zaten `facts.ts`
  metnini gösteriyor, web'in circuit fotoğrafı (`circuitCoverSrc`,
  `02cd51e`) glassmorphic kartla mobile'a taşınacak.
- Kapsam dışı: sezon arşivi, paylaşılabilir kartlar, ana ekran widget'ı
  (önceki turdan zaten ertelenmişti).

**Kritik karar — cron nereye eklenecek:** Efendim sordu, netleştirildi.
`/api/cron/notify-sessions` **Vercel/Next.js'te** yaşayacak (kod
tarafı), ama onu tetikleyen zamanlayıcı **Railway'de** olacak —
`railway/apex-sync-f1-cron/`'daki mevcut kalıbı takip ederek (Vercel
Hobby plan cron'ları günde bir kez sınırlı, 5-10 dk sıklık gerekiyor).
`vercel.json`'a yeni satır eklenmeyecek.

**Durum: Spec onaylandı, uygulamaya HENÜZ BAŞLANMADI.** Efendim "akşam
devam edeceğiz" dedi — bu oturumda aksiyon alınmadı. Sıradaki adım:
writing-plans skill'i ile implementasyon planı çıkarmak, sonra sırayla
uygulamak (önce 13 bug, sonra Grid/Season ayrımı, sonra pist haritaları,
sonra push bildirimleri — cron kısmı Railway'e eklenmeden önce Efendim'e
tekrar haber verilecek).
