# 2026-07-12

- **Yanıltıcı arama ikonu kaldırıldı:** `SiteHeader.tsx` — büyüteç ikonu + `aria-label="Search"` aslında `/season`'a düz linkti (kullanıcı arama bekleyip navigasyon buluyordu). `NAV_ITEMS` zaten `/season` içerdiğinden ikinci bir link eklemek yerine tamamen kaldırıldı; logo genişliğini dengeleyen görünmez bir spacer'a çevrildi (HeaderNav ortalanmış kalıyor).
- **API route entegrasyon testleri eklendi (denetim raporunun son 🟠 maddesi):** Üç yeni dosya, route handler'ları doğrudan çağrılıyor — 54 → 117 test.
  - `tests/api-f1-season.test.ts` (29 test): SSRF whitelist'i. Mutlak URL, protocol-relative `//`, cloud metadata IP'si (`169.254.169.254`), path traversal, listelenmemiş endpoint'ler → hepsi 400 **ve ağa hiç çıkmıyor** (`fetch` mock'lanıp çağrılmadığı doğrulanıyor). İzinli path şekilleri geçiyor, `.json` tam bir kez ekleniyor, baştaki `/` sıyrılıyor. Upstream 404→200/`{MRData:{}}`, 500→502, abort→504; hata gövdelerinde upstream detayı sızmıyor. Sezon-farkındalıklı cache header'ları da pinlendi.
  - `tests/api-push-register.test.ts` (16 test): Bozuk JSON→400, Expo olmayan/eksik/non-string token→400, `preferences` sanitizasyonu (yalnızca boolean değerler tutuluyor, max 20 anahtar, array/string/number/null → `{}`), DB hatasında generic mesaj döndüğü ve Supabase constraint adının sızmadığı (detayın `console.error`'a gittiği) doğrulanıyor.
  - `tests/api-cron-guard.test.ts` (18 test): Üç cron route'unun auth kapısı (header yok / yanlış secret / Bearer şeması yok / hiç secret yapılandırılmamış → hepsi 401) ve trigger throttle'ı (ikinci yetkili çağrı 429; yetkisiz flood throttle'ı tüketmiyor, çünkü auth önce kontrol ediliyor). Throttle modül-seviyesi state tuttuğu için her testte `vi.resetModules()` ile temiz modül alınıyor; dış API'ler (Jolpica/OpenF1/RSS) mock'lanarak testlerin gerçek ağa çıkması engellendi (bu yapılmadan `sync-f1` testleri 5sn timeout'a düşüyordu).
  - **Testlerin gerçekten koruyucu olduğu doğrulandı:** `f1-season`'daki whitelist kontrolü bilerek kaldırılınca 11 test kırıldı (evil.example.com, metadata endpoint'i, path traversal dahil), sonra geri yüklendi.
- **Rapor durumu:** `docs/reference/web-iyilestirme-onerileri-2026-07-05.md` — tüm 🔴/🟠 maddeler artık kapalı. Geriye yalnızca 🟡 borç kalemleri kaldı (RTL component testleri, `rateLimit`'in Upstash yolu, `parseSeason`'ın `?season=`'ı sessizce yok sayması, cron `maxDuration` tekrarı, `frame-ancestors` genişliği, `profileSeasons()` N+1 riski, renk-kodlu SVG `aria-label`'ları).
