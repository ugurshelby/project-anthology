# AGENT — 5FIX2 — 2026-06-09

## Ne yapıldı

### SORUN 1 — next.config.ts: CSP + Image Domain
- `script-src` direktifine `https://vercel.live` eklendi (Vercel Live toolbar).
- `connect-src` direktifine `data:` eklendi.
- `images.remotePatterns` dizisine `{ protocol: 'https', hostname: 'ichef.bbci.co.uk' }` eklendi (BBC görsel CDN).
- Diğer direktiflere dokunulmadı.

### SORUN 2 — /season Driver Standings "Team" kolonu boş ("—")
- **Kök neden:** Mapper (`getDriverStandings`, `lib/f1/mrdata.ts`) `Constructors[0].name`'i doğru okuyor. Sorun veri kaynağında: 2026 `standings_drivers` DB snapshot'ı **F1DB seed** ile yazılmış ve driver satırlarında `Constructors[]` bloğu **boş** geliyor (puanlar + isimler var, takım yok). Zaman-tabanlı staleness kontrolü bu "içerik boş ama taze" satırı yakalamıyor → bozuk DB satırı servis ediliyordu.
- Canlı Jolpica payload'ı doğrulandı: `Constructors[0].name = "Mercedes"` mevcut (curl ile teyit). Yani canlı yol kullanılınca veri tam.
- **Çözüm:** `lib/data/f1.ts`'e `isSeasonSnapshotContentInvalid()` content-validity guard eklendi. Current-season `standings_drivers` snapshot'ında hiçbir satır constructor adı taşımıyorsa → DB satırı atlanır, canlı Jolpica'ya düşülür (fetched_at yaşından bağımsız).

### SORUN 3 — /season Race Calendar kart adı boş
- **Kök neden:** SORUN 2 ile aynı. 2026 `calendar` DB snapshot'ı da F1DB seed ile yazılmış, `raceName: ""` ve `circuitName: ""` boş; `circuitId: "melbourne"` (F1DB id formatı, canlı Jolpica `albert_park` kullanır). Render kodu `race.raceName`'i doğru okuyor ama veri boş geliyordu.
- **Çözüm:** Aynı `isSeasonSnapshotContentInvalid()` guard'ı `calendar` tipi için: tüm yarışlarda `raceName` boşsa → placeholder kabul edilip canlı Jolpica'ya düşülür.

### SORUN 4 — Home "Latest Intel" /news'ten eski kalıyor
- **Kök neden:** Home `getLatestNews(6)` çağırıyordu — bu DB-öncelikli (`news_cache`, cron besler → bayatlayabilir). /news ise `aggregate({ maxItems: 100 })` ile **canlı RSS** çekiyor.
- **Çözüm:** Home da `aggregate({ maxItems: 6 })`'a yönlendirildi — /news ile aynı canlı kaynak. `NewsItem` tipleri yapısal olarak özdeş (lib/data/types vs lib/news/aggregate), TS sorunsuz. Home zaten `revalidate = 0`.

### SORUN 5 — Doğrulama
- `npm run build`: ✓ Compiled successfully, TypeScript ✓, 48/48 sayfa. Sıfır hata.
- `/season` → `ƒ (Dynamic)` ✓
- `/` (home) → `ƒ (Dynamic)` ✓
- Build logunda content-invalid guard çalışıyor: `(calendar content-invalid) → /api/f1-season (proxy)`.

## Değişen dosyalar
- `next.config.ts` (CSP script-src/connect-src, remotePatterns)
- `lib/data/f1.ts` (isSeasonSnapshotContentInvalid + fetchSeasonSnapshotTyped içine wire)
- `app/page.tsx` (getLatestNews → aggregate)

## Çalıştırılan komutlar
- `curl https://api.jolpi.ca/ergast/f1/2026/driverStandings.json` (canlı shape teyidi)
- `curl https://project-anthology-five.vercel.app/api/season/2026` (bozuk DB shape teyidi: constructorName="—", raceName="")
- `npm run build`

## Karşılaşılan hata ve çözüm
- Başta mapper'da bug aranıyordu; mapper doğruydu. Prod API çıktısı incelenince kök neden DB snapshot içeriğinin boş olması olarak tespit edildi. Cron'u buradan tetiklemek yerine read-layer'da content-validity guard ile kalıcı çözüm uygulandı (canlı fallback).

## Sonraki adım
- ⚠️ MANUEL/OPSİYONEL: 2026 sezonu için sync-f1 cron'unu (scope=season) tetikleyerek DB'deki F1DB-placeholder snapshot'larını canlı Jolpica verisiyle güncellemek, her istekte canlı fallback maliyetini ortadan kaldırır. Guard zaten doğru veriyi gösteriyor ama her render'da proxy çağrısı yapıyor.
