# 2026-07-12 (devam 2) — Mobile fazına geçiş, yarım kaldı

## Tamamlanan
- Kalan teknik borç (`parseSeason`, rapor gözden geçirme) → commit `0e4ba2f`, push edildi.
- teams/circuits liste sayfaları incelendi: zaten Apex tasarım diline uygun (TeamCard/DriverCard/CircuitCardView), ek iş gerekmedi → commit `41a692c`, push edildi.

## Mobile keşfi (tamamlandı)
- `mobile/` Expo SDK ~56, React Native 0.85.3, expo-router, NativeWind 4. Zaten bir kez tasarlanıp inşa edilmiş (12-task plan tamamlanmış, commit `7c14742`), kök web'in token sistemiyle (bg/surface/apex-red/Barlow Condensed/JetBrains Mono) paralel bir tema zaten kurulu.
- Backend: `mobile/lib/api.ts` hem kök Next.js `/api/*` route'larını (`/api/season/[year]`, `/api/news`, `/api/push/register`) hem doğrudan Supabase REST'i (`stories` tablosu) kullanıyor. Çalışıyor durumda.
- Ekranlar: Home, Season, Profiles (driver/team toggle), Anthology, News/Glossary — kök web ile paralel, expo-router tabanlı.
- `docs/design/design-styles/` içinde 10 stil dökümanı var: Bento Grid, Brutalist UI, Card-Based UI, Dark Mode First, Editorial UI, Glassmorphism, Minimalism, Neo-Brutalism, Neumorphism, Swiss Design.

## Kritik bulgu — EAS build 858 MB sorununun kök nedeni
`mobile/` klasörü ana repo'nun `.gitignore`'unda (`/mobile/`) — hiç commit edilmemiş, ve kendi bağımsız git reposu da yok. `cd mobile && git rev-parse --show-toplevel` → `anthology/` kök dizinini dönüyor (2.7GB). EAS CLI proje dizinini `mobile/` olarak doğru buluyor ama git root'u yanlış algılıyor, bu da paketleme mantığını bozup upload boyutunu şişiriyor.

**Karar (Efendim onayladı):** `mobile/` için bağımsız bir git reposu oluşturulacak (`cd mobile && git init`). `mobile/.gitignore` zaten doğru (node_modules, .expo, native klasörler hariç) — Expo'nun standart şablonundan geliyor, ek düzenleme gerekmiyor. Ana `anthology` reposuna dokunulmayacak, GitHub'a push gerekmiyor (yerel repo yeterli, EAS zaten yerel diff'i kullanıyor).

## Yarım kalan adım (henüz uygulanmadı)
`git init` komutu kullanıcı tarafından durduruldu (izin istemi reddedildi, "kaldığımız yeri not et yarın devam edeceğiz" dendi) — **hiçbir dosya sisteminde değişiklik yapılmadı**, sadece komut iptal edildi.

## Yarın sıradaki adımlar
1. `mobile/` için `git init` + ilk commit (kullanıcı onayıyla tekrar dene).
2. `eas.json` ve `mobile/lib/api.ts` içindeki ölü URL (`project-anthology-five.vercel.app`) → `project-anthology-seven.vercel.app` olarak güncelle (web tarafında zaten düzeltilmişti, mobile ayrı hardcode).
3. Bağımsız repo sonrası bir test build (`eas build --platform android --profile preview`) ile upload boyutunun düştüğünü doğrula.
4. Tasarım kombinasyonu seç (design-styles içinden — henüz karar verilmedi) ve mobile UI'ı buna göre yeniden tasarla.
5. Tüm ekranları yeni tasarımla güncelle, backend bağlantısını doğrula.
6. APK formatında final build al.
