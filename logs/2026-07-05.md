# 2026-07-05

- **Web denetimi:** Dünkü (2026-07-04/05) commit'ler incelendi — home redesign (Split Cinema/Poster Dense), Vercel deploy fix'leri (lazy Supabase, Sentry soft-fail, force-dynamic, Node 24.x pin), 30dk notify cron kaldırma, mobile/ repo'dan çıkarma.
- **Kapsamlı iyileştirme raporu yazıldı:** `docs/reference/web-iyilestirme-onerileri-2026-07-05.md` — backend/veri, API, UI/UX, test, build/deploy bölümleri; mobile/ kapsam dışı bırakıldı.
- **Kritik bulgu:** `lib/data/siteUrl.ts` fallback sırası bozuk — preview deploy'lar prod API'sinden veri okuyor olabilir.
- **Orta bulgular:** `sync-f1` cron artık hep `season` scope'ta (veri 24 saate kadar bayatlayabilir), `push/register` rate-limit'siz, `jsdom` yanlış dependency kategorisinde, `cron/notify` route'u zombi (kaldırıldı ama hâlâ deploy'da), hiçbir route'ta `loading.tsx`/`error.tsx` yok, tasarım dili anasayfa ile diğer sayfalar arasında bölünmüş.
- **Düzeltmeler uygulandı:** `lib/data/siteUrl.ts` fallback sırası düzeltildi (preview'lar artık kendi `VERCEL_URL`'inden okuyor), `/api/push/register`'a rate-limit + JSON try/catch eklendi, `app/api/cron/notify` zombi route silindi.
- **Hero redesign (Efendim'in UI/UX audit direktifi):** `docs/superpowers/specs/2026-07-05-hero-redesign-design.md` yazıldı ve onaylandı. `ProfileHero.tsx` baştan yazıldı — kart kabuğu kaldırıldı, z-index katmanlı sinematik hero (arka plan numarası → mask-image ile eriyen sürücü/araba görseli → ön plan metin), team hero'da logo + simetrik pilot numaraları eklendi (`flankNumbers`, `logoSrc` prop'ları). Home hero'ya (`PosterHero.tsx`) ek kontrast katmanı eklendi. Drivers grid'de zaten var olan max-width ve avatar aspect-ratio doğrulandı, değişiklik gerekmedi.
- **Kritik bug — sürücü foto karışıklığı (Efendim'in raporu doğrulandı):** `public/drivers/2026/*.svg` içinde 12 dosyanın adı yanlış sürücüye ait fotoğraf taşıyordu (SVG'lerin embedded base64 içeriği tek tek çıkarılıp görsel olarak doğrulandı). Örnek: `russell.svg` → gerçekte Hülkenberg, `hulkenberg.svg` → gerçekte Ocon, `piastri.svg` → gerçekte Hadjar, `hadjar.svg` → gerçekte Lawson, `bottas.svg` → gerçekte Albon, `albon.svg` → gerçekte Bottas, vb. Tüm 12 yanlış eşleşme düzeltildi. Ayrıca `doohan.svg`/`tsunoda.svg` — 2026 grid'inde bu isimler yok (Efendim'in verdiği güncel listeye göre Alpine'da Colapinto, Racing Bulls'ta Lindblad var, roster JSON eskiydi) — bu iki dosya silindi, `assets/data/season-rosters.json` 2026 bölümü ve `data/drivers/index.ts` (lore) Colapinto/Lindblad ile güncellendi. Artık tam 22/22 sürücü, 11 takım doğru eşleşiyor.
- **BoxBox mobile referansı:** `design/boxbox-mobile/` altındaki 15 ekran görüntüsü anlamlı isimlerle yeniden adlandırıldı (Efendim + Gemini'nin `screens-spec.md`'si). Bellek notu eklendi (`anthology-boxbox-mobile-reference`) — bu referanslar önce web mobil responsive'e, `mobile/` (Expo) app'e daha sonra uygulanacak.
- **BoxBox-ilhamlı iyileştirmeler uygulandı:** `docs/superpowers/specs/2026-07-05-boxbox-inspired-improvements-design.md` onaylandı ve uygulandı — (1) Standings lider hero: yeni `StandingsLeaderCard.tsx` (`DriverLeaderCard`/`TeamLeaderCard`) — P1 artık takım rengi glow + portre + büyük puanla ayrı bir kart, hem home (`FlatStandingsList`) hem `/season` (`StandingsCard`) sayfalarında. (2) Haber: yeni `/news/[id]` detay sayfası (`lib/data/news.ts`'e `getNewsById` eklendi), görselsiz haberler listeden tamamen gizleniyor (`hasRealImage` filtresi), `WireItem`/`NewsCard` artık kapak görseli zorunlu gösteriyor, `/news` liste mobilde tek sütun / desktop'ta 3'lü grid. (3) Circuit detay sayfası: mobilde önce veri kartı sonra harita (`order-2 md:order-1` / `order-1 md:order-2`), haritaya hafif Apex Red radial glow eklendi. Tüm değişiklikler `tsc`/`eslint`/54 test ile doğrulandı.
