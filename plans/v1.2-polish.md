# Project Anthology — v1.2 Cila & Devreden İşler Planı

## Context

Masterplan (Phase 0→8) ve v1.1 paketi **fiilen tamamlandı**: site `https://project-anthology-five.vercel.app`
adresinde canlı, günlük cron'lar besliyor (sync-news 06:00 / sync-f1 07:00 / sync-radio 08:00 UTC),
haberler taze (revalidate=0), navbar/hero/mobil yeniden yapıldı, CSP + güvenlik header'ları aktif,
pist karakteristikleri + lastik bölümü + anthology bento eklendi. Bu plan, **eski iki plandan
devreden açık maddeleri** ve **bu oturumdaki genel taramanın bulduğu gerçek iyileştirmeleri** toplar.

CLAUDE.md anayasası geçerli: her adım `npm run build` sıfır hata, `@/lib/f1Calendar` tek F1 kaynağı,
accent yalnız `#ff1801`, 0 radius/shadow, `pre-plans/DESIGN_SYSTEM.md` SSOT. Her büyük adım ayrı commit;
push onayla; faz sonu `logs/AGENT_*.md`.

**Tamamlanma durumu (kod ile doğrulandı):**
- Masterplan Phase 1-7 ✅. Phase 8 (deploy) ✅ — `five` canlı, cron'lar çalışıyor, READY.
- v1.1 Faz A ✅, Faz C (C1 CSP / C2 URL / C3 deploy) ✅, Faz B → yalnız **B3 bento** ✅.
- Devreden açık: v1.1 **B1, B2, B4, B5, B6** + Masterplan **hedefli testler** + **Phase 5 Task 5 görselleri**.

---

## A — Veri Tazeliği (öncelik 1, hızlı kazanç)

> Tarama bulgusu: `/news` statik prerender yüzünden Haziran 4'te donmuştu; `revalidate=0` ile çözüldü.
> **Aynı risk** DB okuyan diğer sayfalarda da var — cron veriyi güncelliyor ama sayfa build-time'da donuyor.

- [ ] **A1** `app/season/page.tsx`: `export const revalidate = 0` (veya ISR `= 900`). `f1_snapshots`
      standings/results/calendar okuyor; cron sonrası taze kalmalı.
- [ ] **A2** `app/circuits/page.tsx`: `revalidate = 0`/`900`. Calendar (`f1_snapshots`) okuyor.
- [ ] **A3** `app/anthology/page.tsx`: `revalidate = 0`/`900`. `stories` + `radio_moments` okuyor (seed
      sonrası yeni içerik için).
- [ ] **A4** Karar: tam dinamik (`0`) mı 15dk ISR (`900`) mı? Öneri: **ISR 900** (cron günde 1 çalışıyor,
      15dk yeterli taze + CDN cache korunur, sunucu yükü düşük). News'te `0` kalır (en kritik tazelik).

**A DoD:** build exit 0 · cron sonrası `/season` `/circuits` `/anthology` yeni veriyi yansıtır · CDN
gereksiz dynamic'e düşmez (ISR seçilirse).

---

## B — Etkileşim Derinliği (v1.1 Faz B devreden)

- [ ] **B1** Homepage News Highlights → 3'lü slide carousel: yeni `components/ui/NewsCarousel.tsx`
      (`'use client'`), aynı anda 3 kart, ok ile kaydır, `prefers-reduced-motion`→anlık. Veri
      `getLatestNews(6)` (mevcut). `app/page.tsx` News Highlights bölümü bununla değişir.
- [ ] **B2** Detay sayfaları ESC + geri butonu: yeni `components/ui/BackButton.tsx` (`'use client'`,
      `useRouter().back()` + `useEffect` ESC listener, `prefers-reduced-motion` nötr). `app/circuits/[id]/page.tsx`
      ve `app/anthology/[slug]/page.tsx`'teki mevcut "← All …" linkinin yanına/yerine.
- [ ] **B4** Season Race Calendar pistleri tıklanabilir: `app/season/page.tsx` calendar kartları
      `<Link href={/circuits/${race.Circuit?.circuitId}}>` ile sarılır (circuitId yoksa link yok).
      Circuits sayfasındaki desenle birebir.
- [ ] **B5** Circuits detay hava durumu: ⚠️ ARAŞTIRMA — Ergast `Location` lat/long sağlıyor mu kontrol et.
      Varsa env-siz **Open-Meteo** (`getCircuitWeather(lat,long)`, server-side, cache `s-maxage`); yoksa
      `data/circuits/facts.ts`'e koordinat ekle (statik, güvenilir). Veri yoksa panel zarifçe gizli.
      `lib/data/circuits.ts` + `app/circuits/[id]/page.tsx`. CSP `connect-src`'e `api.open-meteo.com` ekle.
- [ ] **B6** Tech Glossary terim kartları tıklanabilir + in-place expand: yeni `components/ui/GlossaryCard.tsx`
      (`'use client'`, expand 300ms cubic-bezier(0.77,0,0.18,1)). Mevcut `<dl>` term render'ı kartlara çevrilir.
      (Tyre bölümü zaten görsel + açıklamalı — dokunma.)

**B DoD:** build exit 0 · rotalar 200 · hydration/console temiz · carousel/ESC/back/expand çalışır,
`prefers-reduced-motion` anlık · B5 hava verisi yoksa panel kırılmadan gizli.

---

## C — Kalite & Sağlamlık (tarama bulguları)

- [ ] **C1** Hedefli testler (Masterplan Phase 3 açık maddesi): `lib/news/aggregate.ts` `processFeeds`
      (dedupe/cluster/filter — `old-versions-valuable-files/news.test.ts` port), `roundSuffixToSnapshotType`,
      `isRaceDone`, adapter `toMrData`. ⚠️ Test runner kurulu mu kontrol (Vitest yok → kurulması gerekebilir,
      MANUEL/karar). Minimum: aggregate + temporal saf fonksiyonlar.
- [ ] **C2** a11y geçişi: interaktif `<button>`/`<Link>` odak görünürlüğü, hero/dekoratif görsellerde `alt=""`
      (zaten ✅), carousel/expand'de `aria-expanded`/`aria-label`. `prefers-reduced-motion` page-transition'da
      zaten var → yeni client component'lerde de uygula.
- [ ] **C3** Cron auth doğrulama (Masterplan Phase 8 DoD): ⚠️ MANUEL — Vercel env adı `CRON_SECRET_KEY`
      koddaki ile eşleşiyor mu? Vercel'in otomatik cron'u bu Bearer'ı gönderebilmeli; yoksa zamanlanmış
      tetik 401 alır (manuel curl çalışır ama otomatik çalışmaz). `vercel env ls` ile teyit.
- [ ] **C4** Lighthouse temel ölçüm (Masterplan Phase 7 açık): prod'da LCP/CLS/erişilebilirlik skoru.
      Büyük hero `clamp(...20rem)` + next/image LCP'yi etkiler; `priority` ve boyutları gözden geçir.

**C DoD:** testler yeşil (kurulursa) · a11y belirgin eksik yok · cron auth teyitli · Lighthouse skorları raporlu.

---

## D — İçerik & Görsel (Masterplan Phase 5 Task 5 devreden)

- [ ] **D1** anthology/glossary destek görselleri: Phase 5 Task 5 "ertelendi" idi. Glossary'ye lastik
      görselleri eklendi (✅ bu oturumda). Kalan: anthology story kartlarında görsel kalitesi/kapsama kontrolü
      (`public/stories/` 17 klasör mevcut) — eksik/bozuk hero görseli olan story var mı tara, varsa raporla.
      (Görsel üretimi User sorumluluğu — agent yalnız eksik tespit eder.)

---

## Kritik dosyalar (özet)
- `app/season/page.tsx` — revalidate (A1) + calendar link (B4)
- `app/circuits/page.tsx` — revalidate (A2)
- `app/anthology/page.tsx` — revalidate (A3)
- `app/page.tsx` — NewsCarousel (B1)
- `app/circuits/[id]/page.tsx` — BackButton (B2) + hava durumu (B5)
- `app/anthology/[slug]/page.tsx` — BackButton (B2)
- `app/tech-glossary/page.tsx` — GlossaryCard expand (B6)
- `lib/data/circuits.ts` — getCircuitWeather (B5)
- `data/circuits/facts.ts` — koordinat ekle (B5, gerekirse)
- `next.config.ts` — CSP connect-src open-meteo (B5)
- `components/ui/{NewsCarousel,BackButton,GlossaryCard}.tsx` — **yeni** (B1/B2/B6)
- test dosyaları — **yeni** (C1, runner kurulursa)

## Yeniden kullanılan mevcut yardımcılar
- `getLatestNews` — `@/lib/data/news` (carousel)
- `getF1Context`, `getLastFinishedRace`, `isRaceDone` — `@/lib/f1Calendar`
- `circuitIconSrc`, `getCircuitFacts` — `@/lib/assets/f1-icons`, `@/data/circuits/facts`
- `SafeImage`, `SectionDivider`, `AtmosphericHero`, `PageTransition` (reduced-motion deseni)

## Uçtan uca doğrulama
1. `npm run build` → exit 0, 0 TS hata.
2. Dev :3000 + canlı `five`: 6 rota + circuit/anthology detay → 200.
3. DOM teyidi: carousel/expand/back render; cron sonrası taze veri.
4. `curl -I` güvenlik header'ları korunuyor (CSP'ye open-meteo eklenince).
5. `logs/AGENT_V1.2_*` log; faz başına ayrı commit; push onayla.

## Öncelik sırası önerisi
1. **A (veri tazeliği)** — en küçük diff, en büyük kullanıcı etkisi (cila riski düşük).
2. **B4 + B2** — düşük riskli etkileşim (link + back/ESC).
3. **B1 + B6** — client carousel/expand (orta risk).
4. **B5** — hava durumu (araştırma + dış API + CSP).
5. **C/D** — test/a11y/Lighthouse/görsel denetim (sağlamlaştırma).

## Notlar
- Season standings artık DOLU (canlı 23 satır) — eski "boş standings" sorunu cron ile çözüldü.
- Cron günde 1 çalışıyor (Hobby) → ISR 900 yeterli; tam dinamik gerekmez (news hariç).
- Eski `plans/project-anthology-masterplan.md` ve `plans/v1.1-improvements.md` bu plana devredilip silindi
  (2026-06-07). Mimari kararlar (Karar A-E) `pre-plans/` + kod + `logs/AGENT_*` zincirinde yaşıyor.
